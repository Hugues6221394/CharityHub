<<<<<<< HEAD
@{
    ViewData["Title"] = "Contact";
}

<div class="container">
    <h1>Contact Us</h1>
    <p>For inquiries, please contact us at support@studentcharityhub.com</p>
</div>



=======
@{
    ViewData["Title"] = "Contact";
}
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>@ViewData["Title"] - Student Charity Hub</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" />
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.css">
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600;700;800&display=swap" rel="stylesheet">
    <style>
        :root {
            --coral: #ff5a5f;
            --coral-light: #ff7a7d;
            --teal: #0d9488;
            --cream: #fffaf0;
            --dark: #1f2937;
            --gray: #94a3b8;
        }

        body {
            font-family: 'Inter', sans-serif;
            background: var(--cream);
            color: var(--dark);
            line-height: 1.7;
        }

        /* Hero Header */
        .contact-hero {
            background: linear-gradient(rgba(0,0,0,0.45), rgba(0,0,0,0.65)), url('https://images.unsplash.com/photo-1552664730-d307ca884978?ixlib=rb-4.0.3&auto=format&fit=crop&q=80') center/cover no-repeat;
            min-height: 55vh;
            display: flex;
            align-items: center;
            text-align: center;
            color: white;
            position: relative;
        }

            .contact-hero::before {
                content: '';
                position: absolute;
                inset: 0;
                background: linear-gradient(135deg, rgba(255,90,95,0.75), rgba(13,148,136,0.65));
            }

        .contact-hero-content {
            position: relative;
            z-index: 2;
        }

        .logo-hero {
            width: 85px;
            height: 85px;
            margin: 0 auto 1.5rem;
            background: white;
            color: var(--coral);
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 2.7rem;
            box-shadow: 0 15px 40px rgba(0,0,0,0.4);
        }

        .contact-hero h1 {
            font-size: 3.6rem;
            font-weight: 800;
            letter-spacing: -1px;
            text-shadow: 0 4px 15px rgba(0,0,0,0.5);
        }

        .contact-hero p {
            font-size: 1.35rem;
            max-width: 700px;
            margin: 1.5rem auto;
            opacity: 0.95;
        }

        /* Contact Card */
        .contact-card {
            background: white;
            border-radius: 24px;
            padding: 3rem 2.5rem;
            box-shadow: 0 20px 50px rgba(0,0,0,0.12);
            max-width: 600px;
            margin: -webkit-box-reflect: below 1px linear-gradient(transparent, rgba(0,0,0,0.05));
        }

            .contact-card h2 {
                font-size: 2.2rem;
                font-weight: 800;
                color: var(--dark);
                margin-bottom: 1.5rem;
            }

        .contact-item {
            display: flex;
            align-items: center;
            gap: 18px;
            font-size: 1.15rem;
            margin-bottom: 1.8rem;
            color: var(--dark);
        }

            .contact-item i {
                width: 48px;
                height: 48px;
                background: var(--coral);
                color: white;
                border-radius: 50%;
                display: flex;
                align-items: center;
                justify-content: center;
                font-size: 1.4rem;
                flex-shrink: 0;
            }

            .contact-item a {
                color: var(--coral);
                font-weight: 600;
                text-decoration: none;
                transition: color 0.3s;
            }

                .contact-item a:hover {
                    color: var(--teal);
                }

        .social-links {
            margin-top: 2.5rem;
        }

            .social-links a {
                width: 50px;
                height: 50px;
                background: var(--coral);
                color: white;
                border-radius: 50%;
                display: inline-flex;
                align-items: center;
                justify-content: center;
                font-size: 1.4rem;
                margin: 0 10px;
                transition: all 0.3s ease;
                box-shadow: 0 8px 20px rgba(255,90,95,0.3);
            }

                .social-links a:hover {
                    background: var(--teal);
                    transform: translateY(-8px);
                    box-shadow: 0 15px 30px rgba(13,148,136,0.4);
                }

                .social-links a:nth-child(2) {
                    background: #1da1f2;
                }

                .social-links a:nth-child(3) {
                    background: #e1306c;
                }

       
        {
            font-size: 2.8rem;
        }

        .contact-card {
            padding: 2rem 1.5rem;
        }

        }
    </style>
</head>
<body>

    <!-- Hero Header -->
    <section class="contact-hero">
        <div class="container contact-hero-content">
            <div class="logo-hero">
                <i class="bi bi-mortarboard-fill"></i>
            </div>
            <h1>Contact Us</h1>
            <p>We’re here to help. Reach out anytime — every message matters.</p>
        </div>
    </section>

    <!-- Contact Card -->
    <section class="container py-5">
        <div class="row justify-content-center">
            <div class="col-lg-7">
                <div class="contact-card text-center text-lg-start">
                    <h2>Let’s Connect</h2>

                    <div class="contact-item">
                        <i class="bi bi-envelope-fill"></i>
                        <div>
                            <strong>Email us at</strong><br>
                            <a href="mailto:support@studentcharityhub.com">support@studentcharityhub.com</a>
                        </div>
                    </div>

                    <div class="contact-item">
                        <i class="bi bi-chat-heart-fill"></i>
                        <div>
                            <strong>We typically reply within 24 hours</strong><br>
                            <small class="text-muted">Your questions help us improve lives together</small>
                        </div>
                    </div>

                    <div class="social-links text-center">
                        <p class="mb-3 fw-600">Follow our journey</p>
                        <a href="#" aria-label="Facebook"><i class="bi bi-facebook"></i></a>
                        <a href="#" aria-label="Twitter"><i class="bi bi-twitter"></i></a>
                        <a href="#" aria-label="Instagram"><i class="bi bi-instagram"></i></a>
                    </div>
                </div>
            </div>
        </div>
    </section>

</body>
</html>
>>>>>>> origin/GateraMe
