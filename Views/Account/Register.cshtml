<<<<<<< HEAD
@model RegisterViewModel
@{
    ViewData["Title"] = "Register";
}

<div class="row justify-content-center">
    <div class="col-md-6">
        <div class="card shadow">
            <div class="card-header bg-primary text-white">
                <h3 class="text-center mb-0">Register</h3>
            </div>
            <div class="card-body">
                <form asp-action="Register" method="post">
                    <div asp-validation-summary="ModelOnly" class="text-danger"></div>
                    <div class="row">
                        <div class="col-md-6 mb-3">
                            <label asp-for="FirstName" class="form-label"></label>
                            <input asp-for="FirstName" class="form-control" />
                            <span asp-validation-for="FirstName" class="text-danger"></span>
                        </div>
                        <div class="col-md-6 mb-3">
                            <label asp-for="LastName" class="form-label"></label>
                            <input asp-for="LastName" class="form-control" />
                            <span asp-validation-for="LastName" class="text-danger"></span>
                        </div>
                    </div>
                    <div class="mb-3">
                        <label asp-for="Email" class="form-label"></label>
                        <input asp-for="Email" class="form-control" />
                        <span asp-validation-for="Email" class="text-danger"></span>
                    </div>
                    <div class="mb-3">
                        <label asp-for="Password" class="form-label"></label>
                        <input asp-for="Password" class="form-control" />
                        <span asp-validation-for="Password" class="text-danger"></span>
                    </div>
                    <div class="mb-3">
                        <label asp-for="ConfirmPassword" class="form-label"></label>
                        <input asp-for="ConfirmPassword" class="form-control" />
                        <span asp-validation-for="ConfirmPassword" class="text-danger"></span>
                    </div>
                    <div class="mb-3">
                        <label asp-for="Role" class="form-label"></label>
                        <select asp-for="Role" class="form-select">
                            <option value="Donor">Donor</option>
                            <option value="Student">Student</option>
                        </select>
                        <span asp-validation-for="Role" class="text-danger"></span>
                    </div>
                    <div class="d-grid">
                        <button type="submit" class="btn btn-primary">Register</button>
                    </div>
                </form>
                <hr />
                <div class="text-center mt-2">
                    <p>Or continue with:</p>
                    <form asp-action="ExternalLogin" method="post">
                        <button type="submit" name="provider" value="Google" class="btn btn-outline-danger">
                            <i class="bi bi-google"></i> Continue with Google
                        </button>
                    </form>
                </div>
                <div class="text-center mt-3">
                    <p>Already have an account? <a asp-action="Login">Login here</a></p>
                </div>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}



=======
@model RegisterViewModel
@{
    ViewData["Title"] = "Register";
}
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>@ViewData["Title"] - Student Charity Hub</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" />
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.css">
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600;700;800&display=swap" rel="stylesheet">
    <style>
        :root {
            --coral: #ff5a5f;
            --coral-light: #ff7a7d;
            --teal: #0d9488;
            --cream: #fffaf0;
            --dark: #1f2937;
            --gray: #94a3b8;
        }

        body {
            font-family: 'Inter', sans-serif;
            background: var(--cream);
            color: var(--dark);
            min-height: 100vh;
        }

        /* Full-screen Hero with Diverse Students */
        .register-hero {
            background: linear-gradient(rgba(0,0,0,0.5), rgba(0,0,0,0.7)), url('https://images.unsplash.com/photo-1524178232363-1fb2b075b655?ixlib=rb-4.0.3&auto=format&fit=crop&w=1950&q=80') center/cover no-repeat;
            min-height: 100vh;
            display: flex;
            align-items: center;
            position: relative;
        }

            .register-hero::before {
                content: '';
                position: absolute;
                inset: 0;
                background: linear-gradient(135deg, rgba(255,90,95,0.8), rgba(13,148,136,0.7));
            }

        .hero-content {
            position: relative;
            z-index: 2;
        }

        .logo-hero {
            width: 100px;
            height: 100px;
            margin: 0 auto 2rem;
            background: white;
            color: var(--coral);
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 3.2rem;
            box-shadow: 0 20px 50px rgba(0,0,0,0.4);
        }

        .hero-title {
            font-size: 3.8rem;
            font-weight: 800;
            color: white;
            text-shadow: 0 6px 20px rgba(0,0,0,0.5);
            letter-spacing: -1px;
        }

        .hero-subtitle {
            font-size: 1.4rem;
            color: rgba(255,255,255,0.95);
            max-width: 600px;
            margin: 1.5rem auto;
        }

        /* Register Card - Premium & Trustworthy */
        .register-card {
            background: white;
            border-radius: 28px;
            padding: 3rem;
            box-shadow: 0 30px 80px rgba(0,0,0,0.18);
            max-width: 500px;
            margin: 0 auto;
            position: relative;
            overflow: hidden;
        }

            .register-card::before {
                content: '';
                position: absolute;
                top: 0;
                left: 0;
                right: 0;
                height: 6px;
                background: linear-gradient(90deg, var(--coral), var(--teal));
            }

        .form-label {
            font-weight: 700;
            color: var(--dark);
            font-size: 0.95rem;
        }

        .form-control, .form-select {
            border: 2px solid #e2e8f0;
            border-radius: 16px;
            padding: 0.9rem 1.2rem;
            font-size: 1.05rem;
            transition: all 0.3s ease;
        }

            .form-control:focus, .form-select:focus {
                border-color: var(--coral);
                box-shadow: 0 0 0 5px rgba(255,90,95,0.15);
                transform: translateY(-2px);
            }

        .btn-register {
            background: linear-gradient(135deg, var(--coral), var(--teal));
            color: white;
            font-weight: 800;
            font-size: 1.3rem;
            padding: 1.1rem;
            border-radius: 20px;
            border: none;
            box-shadow: 0 15px 35px rgba(255,90,95,0.4);
            transition: all 0.4s ease;
        }

            .btn-register:hover {
                transform: translateY(-6px);
                box-shadow: 0 25px 50px rgba(255,90,95,0.5);
            }

        .btn-google {
            background: white;
            color: #555;
            border: 2px solid #e2e8f0;
            border-radius: 20px;
            padding: 0.9rem;
            font-weight: 600;
            transition: all 0.3s ease;
        }

            .btn-google:hover {
                background: #db4437;
                color: white;
                border-color: #db4437;
                transform: translateY(-4px);
                box-shadow: 0 15px 30px rgba(219,68,55,0.3);
            }

        .login-link {
            color: var(--coral);
            font-weight: 700;
            text-decoration: none;
        }

            .login-link:hover {
                color: var(--teal);
                text-decoration: underline;
            }

       
        {
            font-size: 2.8rem;
        }

        .register-card {
            padding: 2rem;
            margin: 2rem;
        }

        }
    </style>
</head>
<body>

    <div class="register-hero">
        <div class="container hero-content text-center">
            <div class="logo-hero">
                <i class="bi bi-mortarboard-fill"></i>
            </div>
            <h1 class="hero-title">Join Our Mission</h1>
            <p class="hero-subtitle">
                Become a donor, a student, or a partner — together, we change lives through education.
            </p>

            <div class="register-card mt-5">
                <h3 class="fw-bold text-center mb-4" style="color: var(--dark);">Create Your Account</h3>

                <form asp-action="Register" method="post">
                    <div asp-validation-summary="ModelOnly" class="text-danger text-center mb-3 fw-bold"></div>

                    <div class="row g-3">
                        <div class="col-md-6">
                            <label asp-for="FirstName" class="form-label">First Name</label>
                            <input asp-for="FirstName" class="form-control" placeholder="" />
                            <span asp-validation-for="FirstName" class="text-danger"></span>
                        </div>
                        <div class="col-md-6">
                            <label asp-for="LastName" class="form-label">Last Name</label>
                            <input asp-for="LastName" class="form-control" placeholder="" />
                            <span asp-validation-for="LastName" class="text-danger"></span>
                        </div>
                    </div>

                    <div class="mt-3">
                        <label asp-for="Email" class="form-label">Email Address</label>
                        <input asp-for="Email" class="form-control" placeholder="" />
                        <span asp-validation-for="Email" class="text-danger"></span>
                    </div>

                    <div class="mt-3">
                        <label asp-for="Password" class="form-label">Password</label>
                        <input asp-for="Password" class="form-control" type="password" />
                        <span asp-validation-for="Password" class="text-danger"></span>
                    </div>

                    <div class="mt-3">
                        <label asp-for="ConfirmPassword" class="form-label">Confirm Password</label>
                        <input asp-for="ConfirmPassword" class="form-control" type="password" />
                        <span asp-validation-for="ConfirmPassword" class="text-danger"></span>
                    </div>

                    <div class="mt-3">
                        <label asp-for="Role" class="form-label">I am registering as</label>
                        <select asp-for="Role" class="form-select">
                            <option value="Donor">A Donor (I want to support students)</option>
                            <option value="Student">A Student (I need support)</option>
                        </select>
                    </div>

                    <button type="submit" class="btn btn-register w-100 mt-4">
                        Create Account
                    </button>
                </form>

                <div class="text-center my-4 text-muted">or</div>

                <form asp-action="ExternalLogin" method="post">
                    <button type="submit" name="provider" value="Google"
                            class="btn btn-google w-100 d-flex align-items-center justify-content-center gap-3">
                        <i class="bi bi-google"></i>
                        Continue with Google
                    </button>
                </form>

                <div class="text-center mt-4">
                    <p class="mb-0">
                        Already have an account?
                        <a asp-action="Login" class="login-link">Log in here</a>
                    </p>
                </div>
            </div>
        </div>
    </div>

    @section Scripts {
        @await Html.PartialAsync("_ValidationScriptsPartial")
    }
</body>
</html>
>>>>>>> origin/GateraMe
