<<<<<<< HEAD
@model LoginViewModel
@{
    ViewData["Title"] = "Login";
}

<div class="row justify-content-center">
    <div class="col-md-6">
        <div class="card shadow">
            <div class="card-header bg-primary text-white">
                <h3 class="text-center mb-0">Login</h3>
            </div>
            <div class="card-body">
                <form asp-action="Login" method="post">
                    <div asp-validation-summary="ModelOnly" class="text-danger"></div>
                    <div class="mb-3">
                        <label asp-for="Email" class="form-label"></label>
                        <input asp-for="Email" class="form-control" />
                        <span asp-validation-for="Email" class="text-danger"></span>
                    </div>
                    <div class="mb-3">
                        <label asp-for="Password" class="form-label"></label>
                        <input asp-for="Password" class="form-control" />
                        <span asp-validation-for="Password" class="text-danger"></span>
                    </div>
                    <div class="mb-3 form-check">
                        <input asp-for="RememberMe" class="form-check-input" />
                        <label asp-for="RememberMe" class="form-check-label"></label>
                    </div>
                    <div class="d-grid">
                        <button type="submit" class="btn btn-primary">Login</button>
                    </div>
                </form>
                <hr>
                <div class="text-center">
                    <a asp-action="ForgotPassword">Forgot your password?</a>
                </div>
                <div class="text-center mt-3">
                    <p>Or login with:</p>
                    <form asp-action="ExternalLogin" method="post">
                        <button type="submit" name="provider" value="Google" class="btn btn-outline-danger">
                            <i class="bi bi-google"></i> Google
                        </button>
                    </form>
                </div>
                <div class="text-center mt-3">
                    <p>Don't have an account? <a asp-action="Register">Register here</a></p>
                </div>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}



=======
@model LoginViewModel
@{
    ViewData["Title"] = "Login";
}
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>@ViewData["Title"] - Student Charity Hub</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" />
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.css">
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600;700;800&display=swap" rel="stylesheet">
    <style>
        :root {
            --coral: #ff5a5f;
            --coral-light: #ff7a7d;
            --teal: #0d9488;
            --cream: #fffaf0;
            --dark: #1f2937;
            --gray: #94a3b8;
        }

        body {
            font-family: 'Inter', sans-serif;
            background: var(--cream);
            color: var(--dark);
            min-height: 100vh;
        }

        /* Full-screen Hero with Diverse Students */
        .login-hero {
            background: linear-gradient(rgba(0,0,0,0.55), rgba(0,0,0,0.75)), url('https://images.unsplash.com/photo-1522202176988-66273c2fd55f?ixlib=rb-4.0.3&auto=format&fit=crop&w=1950&q=80') center/cover no-repeat;
            min-height: 100vh;
            display: flex;
            align-items: center;
            position: relative;
        }

            .login-hero::before {
                content: '';
                position: absolute;
                inset: 0;
                background: linear-gradient(135deg, rgba(255,90,95,0.82), rgba(13,148,136,0.72));
            }

        .hero-content {
            position: relative;
            z-index: 2;
            text-align: center;
        }

        .logo-hero {
            width: 100px;
            height: 100px;
            margin: 0 auto 2rem;
            background: white;
            color: var(--coral);
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 3.2rem;
            box-shadow: 0 20px 50px rgba(0,0,0,0.45);
        }

        .hero-title {
            font-size: 3.8rem;
            font-weight: 800;
            color: white;
            text-shadow: 0 6px 20px rgba(0,0,0,0.6);
            letter-spacing: -1px;
        }

        .hero-subtitle {
            font-size: 1.4rem;
            color: rgba(255,255,255,0.95);
            max-width: 600px;
            margin: 1.5rem auto;
        }

        /* Login Card */
        .login-card {
            background: white;
            border-radius: 28px;
            padding: 3rem;
            box-shadow: 0 30px 80px rgba(0,0,0,0.2);
            max-width: 480px;
            margin: 0 auto;
            position: relative;
            overflow: hidden;
        }

            .login-card::before {
                content: '';
                position: absolute;
                top: 0;
                left: 0;
                right: 0;
                height: 6px;
                background: linear-gradient(90deg, var(--coral), var(--teal));
            }

        .form-label {
            font-weight: 700;
            color: var(--dark);
            font-size: 0.95rem;
        }

        .form-control {
            border: 2px solid #e2e8f0;
            border-radius: 16px;
            padding: 0.9rem 1.2rem;
            font-size: 1.05rem;
            transition: all 0.3s ease;
        }

            .form-control:focus {
                border-color: var(--coral);
                box-shadow: 0 0 0 5px rgba(255,90,95,0.15);
                transform: translateY(-2px);
            }

        .btn-login {
            background: linear-gradient(135deg, var(--coral), var(--teal));
            color: white;
            font-weight: 800;
            font-size: 1.3rem;
            padding: 1.1rem;
            border-radius: 20px;
            border: none;
            box-shadow: 0 15px 35px rgba(255,90,95,0.4);
            transition: all 0.4s ease;
        }

            .btn-login:hover {
                transform: translateY(-6px);
                box-shadow: 0 25px 50px rgba(255,90,95,0.5);
            }

        .btn-google {
            background: white;
            color: #555;
            border: 2px solid #e2e8f0;
            border-radius: 20px;
            padding: 0.9rem;
            font-weight: 600;
            transition: all 0.3s ease;
        }

            .btn-google:hover {
                background: #db4437;
                color: white;
                border-color: #db4437;
                transform: translateY(-4px);
                box-shadow: 0 15px 30px rgba(219,68,55,0.3);
            }

        .form-check-input:checked {
            background-color: var(--teal);
            border-color: var(--teal);
        }

        .link-coral {
            color: var(--coral);
            font-weight: 700;
            text-decoration: none;
        }

            .link-coral:hover {
                color: var(--teal);
                text-decoration: underline;
            }

        
        {
            font-size: 2.8rem;
        }

        .login-card {
            padding: 2rem;
            margin: 2rem;
        }

        }
    </style>
</head>
<body>

    <div class="login-hero">
        <div class="container hero-content">
            <div class="logo-hero">
                <i class="bi bi-mortarboard-fill"></i>
            </div>
            <h1 class="hero-title">Welcome Back</h1>
            <p class="hero-subtitle">
                Log in to continue supporting students or managing your sponsorship.
            </p>

            <div class="login-card mt-5">
                <form asp-action="Login" method="post">
                    <div asp-validation-summary="ModelOnly" class="text-danger text-center mb-3 fw-bold"></div>

                    <div class="mb-4">
                        <label asp-for="Email" class="form-label">Email Address</label>
                        <input asp-for="Email" class="form-control" placeholder="you@example.com" />
                        <span asp-validation-for="Email" class="text-danger"></span>
                    </div>

                    <div class="mb-4">
                        <label asp-for="Password" class="form-label">Password</label>
                        <input asp-for="Password" class="form-control" type="password" />
                        <span asp-validation-for="Password" class="text-danger"></span>
                    </div>

                    <div class="form-check mb-4">
                        <input asp-for="RememberMe" class="form-check-input" />
                        <label asp-for="RememberMe" class="form-check-label">Remember me</label>
                    </div>

                    <button type="submit" class="btn btn-login w-100">
                        Log In
                    </button>
                </form>

                <div class="text-center my-4">
                    <a asp-action="ForgotPassword" class="link-coral">Forgot your password?</a>
                </div>

                <div class="text-center my-4 text-muted fw-medium">or</div>

                <form asp-action="ExternalLogin" method="post">
                    <button type="submit" name="provider" value="Google"
                            class="btn btn-google w-100 d-flex align-items-center justify-content-center gap-3">
                        <i class="bi bi-google"></i>
                        Continue with Google
                    </button>
                </form>

                <div class="text-center mt-4">
                    <p class="mb-0">
                        New here?
                        <a asp-action="Register" class="link-coral">Create an account</a>
                    </p>
                </div>
            </div>
        </div>
    </div>

    @section Scripts {
        @await Html.PartialAsync("_ValidationScriptsPartial")
    }
</body>
</html>
>>>>>>> origin/GateraMe
