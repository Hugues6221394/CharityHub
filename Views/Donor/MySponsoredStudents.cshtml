<<<<<<< HEAD
@model IEnumerable<StudentCharityHub.Models.Student>
@{
    ViewData["Title"] = "My Sponsored Students";
}

<div class="container">
    <h1 class="mb-4">My Sponsored Students</h1>
    <div class="row">
        @if (Model.Any())
        {
            foreach (var student in Model)
            {
                <div class="col-md-4 mb-4">
                    <div class="card h-100 shadow-sm">
                        @if (!string.IsNullOrEmpty(student.PhotoUrl))
                        {
                            <img src="@student.PhotoUrl" class="card-img-top" alt="@student.FullName"
                                 style="height: 220px; object-fit: cover;">
                        }
                        <div class="card-body d-flex flex-column">
                            <h5 class="card-title">@student.FullName</h5>
                            <p class="card-text"><i class="bi bi-geo-alt"></i> @student.Location</p>
                            <p class="card-text"><strong>Goal:</strong> $@student.FundingGoal.ToString("F2")</p>
                            <p class="card-text"><strong>Raised:</strong> $@student.AmountRaised.ToString("F2")</p>
                            <div class="mt-auto">
                                <a asp-controller="Student" asp-action="Details" asp-route-id="@student.Id"
                                   class="btn btn-primary w-100 mb-2">View Profile</a>
                                <a asp-controller="Donation" asp-action="Create" asp-route-studentId="@student.Id"
                                   class="btn btn-outline-success w-100">Donate Again</a>
                                <a asp-controller="Messages" asp-action="Create" asp-route-studentId="@student.Id"
                                   class="btn btn-outline-secondary w-100 mt-2">Message Student</a>
                            </div>
                        </div>
                    </div>
                </div>
            }
        }
        else
        {
            <div class="col-12">
                <div class="alert alert-info">
                    You haven't sponsored any students yet. <a asp-controller="Student" asp-action="Index">Browse students</a>
                    to start making an impact.
                </div>
            </div>
        }
    </div>
</div>

=======
@model IEnumerable<StudentCharityHub.Models.Student>
@{
    ViewData["Title"] = "My Sponsored Students";
}
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>@ViewData["Title"] - Student Charity Hub</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" />
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.css">
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">
    <style>
        :root {
            --coral: #ff5a5f;
            --teal: #0d9488;
            --cream: #fffaf0;
            --dark: #1f2937;
        }

        body {
            font-family: 'Inter', sans-serif;
            background: var(--cream);
            color: var(--dark);
            font-size: 0.95rem;
        }

        a {
            text-decoration: none !important;
        }

        /* Hero */
        .page-hero {
            background: linear-gradient(rgba(0,0,0,0.55), rgba(0,0,0,0.75)), url('https://images.unsplash.com/photo-1593113598332-cd288d649433?auto=format&fit=crop&w=1950&q=80') center/cover;
            min-height: 300px;
            display: flex;
            align-items: center;
            border-radius: 0 0 32px 32px;
            position: relative;
            overflow: hidden;
        }

            .page-hero::before {
                content: '';
                position: absolute;
                inset: 0;
                background: linear-gradient(135deg, rgba(255,90,95,0.88), rgba(13,148,136,0.82));
            }

        .hero-content {
            position: relative;
            text-align: center;
            color: white;
        }

        .hero-title {
            font-size: 2.6rem;
            font-weight: 700;
            margin-bottom: 0.5rem;
        }

        .hero-subtitle {
            font-size: 1.2rem;
            opacity: 0.95;
        }

        /* Student Cards */
        .student-card {
            background: white;
            border-radius: 24px;
            overflow: hidden;
            box-shadow: 0 12px 35px rgba(0,0,0,0.09);
            transition: all 0.35s ease;
            height: 100%;
        }

            .student-card:hover {
                transform: translateY(-10px);
                box-shadow: 0 25px 60px rgba(255,90,95,0.18);
            }

        .card-img-top {
            height: 240px;
            object-fit: cover;
        }

        .card-body {
            padding: 1.8rem;
        }

        .card-title {
            font-size: 1.35rem;
            font-weight: 700;
            margin-bottom: 0.8rem;
        }

        .btn-primary {
            background: linear-gradient(135deg, var(--coral), var(--teal));
            border: none;
            border-radius: 16px;
            font-weight: 600;
            padding: 0.75rem;
        }

        .btn-outline-success, .btn-outline-secondary {
            border-radius: 16px;
            font-weight: 500;
        }

        .empty-state {
            background: white;
            border-radius: 24px;
            padding: 4rem 2rem;
            box-shadow: 0 12px 35px rgba(0,0,0,0.09);
            text-align: center;
        }

            .empty-state i {
                font-size: 4.5rem;
                color: #cbd5e1;
                margin-bottom: 1.5rem;
            }

       

        {
            font-size: 2.2rem;
        }

        .student-card {
            margin-bottom: 1.5rem;
        }

        }
    </style>
</head>
<body>

    <!-- Hero -->
    <section class="page-hero">
        <div class="container hero-content">
            <h1 class="hero-title">My Sponsored Students</h1>
            <p class="hero-subtitle">The lives you’re changing — forever</p>
        </div>
    </section>

    <div class="container py-5">
        <div class="row g-4 justify-content-center">

            @if (Model.Any())
            {
                @foreach (var student in Model)
                {
                    <div class="col-md-6 col-lg-4">
                        <div class="student-card">
                            @if (!string.IsNullOrEmpty(student.PhotoUrl))
                            {
                                <img src="@student.PhotoUrl" class="card-img-top" alt="@student.FullName">
                            }
                            else
                            {
                                <div class="bg-light text-center py-5">
                                    <i class="bi bi-person-circle" style="font-size: 6rem; color: #cbd5e1;"></i>
                                </div>
                            }

                            <div class="card-body d-flex flex-column">
                                <h5 class="card-title">@student.FullName</h5>
                                <p class="text-muted mb-2">
                                    <i class="bi bi-geo-alt me-1"></i>@student.Location • Age @student.Age
                                </p>
                                <div class="mb-3">
                                    <small class="text-muted d-block">Funding Goal</small>
                                    <strong class="fs-5">$@student.FundingGoal.ToString("N0")</strong>
                                </div>
                                <div class="mb-4">
                                    <div class="progress" style="height: 8px;">
                                        <div class="progress-bar bg-success" role="progressbar"
                                             style="width: @(student.FundingGoal > 0 ? (student.AmountRaised / student.FundingGoal * 100) : 0)%">
                                        </div>
                                    </div>
                                    <small class="text-success fw-semibold mt-1 d-block">
                                        $@student.AmountRaised.ToString("N0") raised
                                    </small>
                                </div>

                                <div class="mt-auto d-grid gap-2">
                                    <a asp-controller="Student" asp-action="Details" asp-route-id="@student.Id"
                                       class="btn btn-primary">
                                        View Full Profile
                                    </a>
                                    <a asp-controller="Donation" asp-action="Create" asp-route-studentId="@student.Id"
                                       class="btn btn-outline-success">
                                        Donate Again
                                    </a>
                                    <a asp-controller="Messages" asp-action="Create" asp-route-studentId="@student.Id"
                                       class="btn btn-outline-secondary">
                                        Send Message
                                    </a>
                                </div>
                            </div>
                        </div>
                    </div>
                }
            }
            else
            {
                <div class="col-lg-8">
                    <div class="empty-state">
                        <i class="bi bi-heart"></i>
                        <h4 class="mt-4 mb-3">You haven’t sponsored anyone yet</h4>
                        <p class="text-muted mb-4">
                            When you make your first donation, the student will appear here — and you’ll be able to follow their journey forever.
                        </p>
                        <a asp-controller="Student" asp-action="Index" class="btn btn-primary btn-lg px-5 py-3">
                            Browse Students to Support
                        </a>
                    </div>
                </div>
            }

        </div>
    </div>

</body>
</html>
>>>>>>> origin/GateraMe
